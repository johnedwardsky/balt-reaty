# Balthomes - Система управления недвижимостью

## 🚀 Быстрый старт

### Запуск админ-панели

```bash
cd /Users/johnsky/.gemini/antigravity/scratch/kaliningrad-real-estate/
python3 admin_server.py
```

Затем откройте в браузере: **http://localhost:3001/admin**

---

## 📝 Рабочий процесс

### 1. Редактирование объектов
- Откройте админку: `http://localhost:3001/admin`
- Нажмите на иконку ✏️ для редактирования объекта
- Измените данные на всех языках (RU, EN, DE, ZH)
- Нажмите **"Сохранить"**

### 2. Добавление новых объектов
- Нажмите кнопку **"+ Новый объект"**
- Заполните все поля
- Загрузите фотографии
- Сохраните

### 3. Публикация изменений на сайт

После редактирования в админке:

```bash
# Шаг 1: Нажмите в админке "Опубликовать страницы"
# Это создаст HTML-файлы для всех объектов

# Шаг 2: Отправьте изменения на GitHub
cd /Users/johnsky/.gemini/antigravity/scratch/kaliningrad-real-estate/
git add .
git commit -m "Обновление каталога"
git push origin main
```

**Сайт обновится автоматически через 1-2 минуты!**

---

## 🌐 Адреса

- **Живой сайт**: https://balthomes.ru
- **Резервный адрес**: https://johnedwardsky.github.io/Balthomes/
- **Админка (локально)**: http://localhost:3001/admin
- **GitHub репозиторий**: https://github.com/johnedwardsky/Balthomes

---

## 📂 Структура проекта

```
kaliningrad-real-estate/
├── admin/              # Интерфейс админ-панели
│   └── index.html
├── admin_server.py     # Бэкенд сервер (Flask)
├── data.json          # База данных объектов
├── js/
│   └── properties.js  # Данные для сайта (синхронизируется автоматически)
├── images/            # Фотографии объектов
├── templates/         # Шаблоны для генерации страниц
│   └── object-template.html
├── index.html         # Главная страница (RU)
├── en.html           # Английская версия
├── de.html           # Немецкая версия
└── zh.html           # Китайская версия
```

---

## 🔧 Технические детали

### База данных
- **Основной источник**: `data.json`
- **Для сайта**: `js/properties.js` (обновляется автоматически при сохранении)

### Языки
- 🇷🇺 Русский (RU) - основной
- 🇬🇧 Английский (EN)
- 🇩🇪 Немецкий (DE)
- 🇨🇳 Китайский (ZH)

### Автоматическая генерация страниц
При нажатии "Опубликовать страницы" создаются:
- `object-{id}.html` (русская версия)
- `object-{id}-en.html` (английская)
- `object-{id}-de.html` (немецкая)
- `object-{id}-zh.html` (китайская)

---

## 🆘 Решение проблем

### Админка не открывается
```bash
# Проверьте, запущен ли сервер
lsof -ti :3001

# Если порт занят, перезапустите
lsof -ti :3001 | xargs kill -9
python3 admin_server.py
```

### Сайт не обновляется
```bash
# Проверьте статус Git
git status

# Убедитесь, что изменения отправлены
git log -1

# Принудительная отправка (осторожно!)
git push origin main --force
```

### Домен balthomes.ru не работает
1. Зайдите в [настройки GitHub Pages](https://github.com/johnedwardsky/Balthomes/settings/pages)
2. В поле **Custom domain** введите: `balthomes.ru`
3. Нажмите **Save**
4. Подождите 5-10 минут

---

## 📦 Зависимости

Для работы админки требуется Python 3 и следующие библиотеки:

```bash
pip3 install -r requirements.txt
```

Содержимое `requirements.txt`:
- Flask==3.0.0
- flask-cors==4.0.0
- Werkzeug==3.0.1

---

## 💡 Полезные команды

```bash
# Просмотр всех объектов в базе
cat data.json | python3 -m json.tool

# Резервная копия базы данных
cp data.json data.json.backup

# Просмотр истории изменений
git log --oneline

# Откат к предыдущей версии (если что-то пошло не так)
git reset --hard HEAD~1
```

---

## 📞 Поддержка

Если возникнут вопросы или проблемы, обращайтесь к разработчику или проверьте документацию на GitHub.

**Удачи в продажах! 🏡✨**
