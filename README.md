# 🏡 BaltHomes - Система управления недвижимостью

Многоязычный сайт недвижимости с админ-панелью и автоматическим переводом.

## ✨ Возможности

### 🎯 Админ-панель
- ✅ Управление объектами недвижимости
- ✅ Поддержка разных типов: **Дома**, **Квартиры**, **Таунхаусы**
- ✅ Динамические поля в зависимости от типа
- ✅ Загрузка и сортировка фотографий
- ✅ Drag & Drop для изменения порядка

### 🌍 Автоматический перевод
- ✅ Заполняете данные **только на русском**
- ✅ Автоматический перевод на **EN, DE, ZH** через Google Translate
- ✅ **Полностью БЕСПЛАТНО**
- ✅ **Без API ключей** - работает сразу!

### 🏠 Типы недвижимости

#### Дома / Коттеджи
- Площадь дома (м²)
- Площадь участка (соток)
- Количество этажей
- Количество комнат
- Материал стен

#### Квартиры / Апартаменты
- Площадь (м²)
- Этаж / Этажность (5/9)
- Количество комнат
- Подъезд
- Балкон / Лоджия
- Отопление (центральное/автономное/газовое)
- Ремонт (без ремонта/косметический/евроремонт)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd /Users/johnsky/Documents/balthomes.ru
pip3 install googletrans==4.0.0rc1
```

### 2. Запуск админ-панели

```bash
python3 admin_server.py
```

Откройте: **http://localhost:8000/admin**

### 3. Работа с объектами

1. **Создайте/Редактируйте объект** в админке
2. **Выберите тип недвижимости** (дом/квартира)
3. **Заполните данные на русском**
4. **Сохраните** - данные попадут в `data.json`

### 4. Генерация страниц с автопереводом

```bash
python3 generate_all_pages.py
```

Скрипт автоматически:
- 🌐 Переведет все тексты на EN, DE, ZH
- 📄 Создаст HTML страницы для всех языков
- 🗑️ Удалит устаревшие страницы

### 5. Публикация на GitHub Pages

Через админку нажмите **"Отправить на сайт"** или:

```bash
git add .
git commit -m "Update properties"
git push origin main
```

## 📁 Структура проекта

```
kaliningrad-real-estate/
├── admin_server.py          # Flask сервер для админки
├── generate_all_pages.py    # Генератор страниц + автоперевод
├── data.json                 # База данных объектов
├── templates/
│   ├── admin.html           # Админ-панель
│   └── full-object-template.html  # Шаблон страницы объекта
├── images/
│   └── object-{id}/         # Фото каждого объекта
├── index.html               # Главная (RU)
├── en.html                  # Главная (EN)
├── de.html                  # Главная (DE)
├── zh.html                  # Главная (ZH)
└── object-{id}-{lang}.html  # Страницы объектов
```

## 🎨 Особенности дизайна

- ✅ Адаптивный дизайн (desktop + mobile)
- ✅ Переключатель языков в хедере
- ✅ Гамбургер-меню на мобильных
- ✅ Модальная галерея с навигацией
- ✅ Премиум UI/UX

## 🔧 API Endpoints

### Админ-панель
- `GET /admin` - Админ-панель
- `GET /api/properties` - Получить все объекты
- `POST /api/properties` - Сохранить объекты
- `POST /api/upload` - Загрузить фото
- `POST /api/generate-pages` - Генерация страниц
- `POST /api/deploy` - Деплой на GitHub

## 📊 Формат данных

```json
{
  "id": 4,
  "type": "house",
  "title": {
    "ru": "Дом в Зеленоградске",
    "en": "House in Zelenogradsk",
    "de": "Haus in Zelenogradsk",
    "zh": "泽列诺格勒茨克的房子"
  },
  "specs": {
    "ru": {
      "houseArea": "300",
      "landArea": "15",
      "floors": "2",
      "rooms": "5+",
      "material": "Кирпич"
    }
  }
}
```

## 💡 Советы

1. **Заполняйте только русский язык** - переводы создаются автоматически
2. **Используйте качественные фото** - первое фото = обложка
3. **Проверяйте переводы** в `data.json` после генерации
4. **Сохраняйте порядок** объектов через drag & drop

## 🐛 Решение проблем

### Переводы не работают
```bash
# Переустановите googletrans
pip3 install --upgrade googletrans==4.0.0rc1
```

### Фото не загружаются
```bash
# Проверьте права на папку
chmod -R 755 images/
```

## 📞 Контакты

- **Сайт**: https://balthomes.ru
- **Email**: info@balthomes.ru
- **Телефон**: +7 905-249-17-55

---

**Создано с ❤️ для элитной недвижимости Калининграда**
