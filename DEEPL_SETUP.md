# Настройка автоматического перевода через DeepL API

## 🌍 Что это дает?

Профессиональный автоматический перевод всех текстов объектов недвижимости на:
- 🇬🇧 **Английский** (EN)
- 🇩🇪 **Немецкий** (DE)  
- 🇨🇳 **Китайский** (ZH)

## 📋 Шаги настройки

### 1. Регистрация в DeepL

1. Перейдите на https://www.deepl.com/pro-api
2. Нажмите **"Sign up for free"** (Бесплатный план)
3. Заполните форму регистрации
4. Подтвердите email

### 2. Получение API ключа

1. Войдите в аккаунт DeepL
2. Перейдите в **Account** → **API Keys**
3. Скопируйте ваш **Authentication Key**

### 3. Установка ключа

**Вариант А: Через переменную окружения (рекомендуется)**

```bash
# macOS/Linux
export DEEPL_API_KEY="ваш-ключ-здесь"

# Или добавьте в ~/.zshrc для постоянного использования:
echo 'export DEEPL_API_KEY="ваш-ключ-здесь"' >> ~/.zshrc
source ~/.zshrc
```

**Вариант Б: Прямо в коде**

Откройте `generate_all_pages.py` и замените строку 13:

```python
DEEPL_API_KEY = os.getenv('DEEPL_API_KEY', 'ваш-ключ-здесь')
```

### 4. Установка зависимостей

```bash
cd /Users/johnsky/.gemini/antigravity/scratch/kaliningrad-real-estate
pip3 install requests
```

## 🚀 Использование

### Автоматический перевод при генерации страниц:

```bash
python3 generate_all_pages.py
```

Скрипт автоматически:
1. ✅ Переведет все русские тексты на EN, DE, ZH
2. ✅ Сохранит переводы в `data.json`
3. ✅ Создаст HTML страницы для всех языков

### Что переводится автоматически:

- **Название объекта** (title)
- **Описание** (description)
- **Локация** (location)
- **Преимущества** (features)
- **Характеристики** (specs) - текстовые значения

### Что НЕ переводится:

- Цены (остаются как есть)
- Числовые значения (площадь, этажи и т.д.)
- Изображения

## 💰 Лимиты бесплатного плана

- **500,000 символов/месяц** бесплатно
- Этого достаточно для ~100-200 объектов недвижимости
- Не требуется кредитная карта

## 🔧 Альтернативы DeepL

Если не хотите использовать DeepL, можно использовать:

### Google Translate API (платный)
```python
# Установка
pip3 install google-cloud-translate

# Требует настройки Google Cloud проекта
```

### Yandex Translate API (бесплатный лимит)
```python
# Бесплатно до 1M символов/месяц
# https://cloud.yandex.ru/services/translate
```

## ⚠️ Важно

- Переводы сохраняются в `data.json` и используются повторно
- При повторном запуске скрипт НЕ переводит заново (экономит API запросы)
- Для принудительного перевода используйте параметр `force_retranslate=True`

## 🧪 Тестирование

Проверьте работу перевода:

```bash
# Запустите скрипт
python3 generate_all_pages.py

# Проверьте data.json - должны появиться переводы
cat data.json | grep -A 5 '"title"'
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что API ключ установлен правильно
2. Убедитесь, что `requests` установлен: `pip3 list | grep requests`
3. Проверьте лимиты в аккаунте DeepL

---

**Готово!** Теперь при заполнении данных в админке на русском языке, все переводы будут создаваться автоматически! 🎉
