# Настройка автоматического перевода через Yandex Cloud Translate

## 🌍 Что это дает?

Профессиональный автоматический перевод всех текстов объектов недвижимости на:
- 🇬🇧 **Английский** (EN)
- 🇩🇪 **Немецкий** (DE)  
- 🇨🇳 **Китайский** (ZH)

## 💰 Преимущества Yandex Cloud

- ✅ **1 млн символов/месяц БЕСПЛАТНО**
- ✅ Отличное качество для русского языка
- ✅ Быстрая работа
- ✅ Простая настройка

## 📋 Шаги настройки

### 1. Создание аккаунта Yandex Cloud

1. Перейдите на https://cloud.yandex.ru
2. Нажмите **"Войти"** или **"Зарегистрироваться"**
3. Войдите через Яндекс ID (или создайте новый аккаунт)
4. Активируйте пробный период (дает 4000₽ на 60 дней)

### 2. Создание каталога

1. В консоли Yandex Cloud нажмите **"Создать каталог"**
2. Введите имя: `balthomes` (или любое другое)
3. Нажмите **"Создать"**
4. **Скопируйте ID каталога** (понадобится позже)
   - Он выглядит так: `b1g2abc3def4ghi5jklm`

### 3. Создание API ключа

1. В меню слева выберите **"Сервисные аккаунты"**
2. Нажмите **"Создать сервисный аккаунт"**
3. Введите имя: `translator`
4. Добавьте роль: **`ai.translate.user`**
5. Нажмите **"Создать"**
6. Откройте созданный аккаунт
7. Нажмите **"Создать новый ключ"** → **"Создать API-ключ"**
8. **Скопируйте API-ключ** (он показывается только один раз!)
   - Он выглядит так: `AQVNabcdefghijklmnopqrstuvwxyz1234567890`

### 4. Установка ключей

**Вариант А: Через переменные окружения (рекомендуется)**

```bash
# macOS/Linux
export YANDEX_API_KEY="ваш-api-ключ"
export YANDEX_FOLDER_ID="ваш-folder-id"

# Для постоянного использования добавьте в ~/.zshrc:
echo 'export YANDEX_API_KEY="ваш-api-ключ"' >> ~/.zshrc
echo 'export YANDEX_FOLDER_ID="ваш-folder-id"' >> ~/.zshrc
source ~/.zshrc
```

**Вариант Б: Прямо в коде**

Откройте `generate_all_pages.py` и замените строки 14-15:

```python
YANDEX_API_KEY = os.getenv('YANDEX_API_KEY', 'ваш-api-ключ')
YANDEX_FOLDER_ID = os.getenv('YANDEX_FOLDER_ID', 'ваш-folder-id')
```

### 5. Установка зависимостей

```bash
cd /Users/johnsky/Documents/balthomes.ru
pip3 install requests
```

## 🚀 Использование

### Автоматический перевод при генерации страниц:

```bash
python3 generate_all_pages.py
```

Скрипт автоматически:
1. ✅ Переведет все русские тексты на EN, DE, ZH
2. ✅ Сохранит переводы в `data.json`
3. ✅ Создаст HTML страницы для всех языков

### Что переводится автоматически:

- **Название объекта** (title)
- **Описание** (description)
- **Локация** (location)
- **Преимущества** (features)
- **Характеристики** (specs) - текстовые значения

### Что НЕ переводится:

- Цены (остаются как есть)
- Числовые значения (площадь, этажи и т.д.)
- Изображения

## 💰 Тарифы Yandex Cloud Translate

### Бесплатный лимит
- **1,000,000 символов/месяц** - БЕСПЛАТНО
- Этого достаточно для **~500 объектов** недвижимости в месяц

### Платный тариф (после превышения лимита)
- **1₽ за 1 млн символов** (очень дешево!)
- Примерно **0.001₽ за один объект**

## 🧪 Тестирование

Проверьте работу перевода:

```bash
# 1. Проверьте переменные окружения
echo $YANDEX_API_KEY
echo $YANDEX_FOLDER_ID

# 2. Запустите скрипт
python3 generate_all_pages.py

# 3. Проверьте data.json - должны появиться переводы
cat data.json | grep -A 5 '"title"'
```

## 📊 Пример вывода

```
🌍 Начинаем автоматический перевод...

📝 Объект 4: Дом в Зеленоградске
  🌐 Переводим title на EN...
  🌐 Переводим title на DE...
  🌐 Переводим title на ZH...
  🌐 Переводим description на EN...
  ...

✅ Переводы сохранены в data.json
📄 Генерируем HTML страницы...
✅ Генерация завершена
```

## ⚠️ Важно

- Переводы сохраняются в `data.json` и используются повторно
- При повторном запуске скрипт НЕ переводит заново (экономит API запросы)
- Для принудительного перевода используйте параметр `force_retranslate=True`

## 🔧 Решение проблем

### Ошибка: "YANDEX_API_KEY не установлен"

```bash
# Проверьте переменные
env | grep YANDEX

# Установите заново
export YANDEX_API_KEY="ваш-ключ"
export YANDEX_FOLDER_ID="ваш-id"
```

### Ошибка: "Unauthorized" (401)

- Проверьте правильность API ключа
- Убедитесь, что сервисному аккаунту назначена роль `ai.translate.user`

### Ошибка: "Folder not found" (404)

- Проверьте правильность FOLDER_ID
- Убедитесь, что каталог существует в вашем облаке

## 📞 Полезные ссылки

- **Консоль Yandex Cloud**: https://console.cloud.yandex.ru
- **Документация API**: https://cloud.yandex.ru/docs/translate/
- **Тарифы**: https://cloud.yandex.ru/docs/translate/pricing

## 🎯 Сравнение с DeepL

| Параметр | Yandex Cloud | DeepL |
|----------|--------------|-------|
| Бесплатный лимит | 1M символов/мес | 500K символов/мес |
| Качество для RU | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Скорость | Быстро | Средне |
| Регистрация | Простая | Требует карты |

---

**Готово!** Теперь при заполнении данных в админке на русском языке, все переводы будут создаваться автоматически через Yandex Cloud! 🎉
