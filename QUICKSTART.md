# 🚀 Быстрый старт - Шпаргалка

## 1️⃣ Настройка Yandex Cloud (один раз)

### Получение ключей:
1. Зайдите на https://cloud.yandex.ru
2. Создайте каталог → скопируйте **FOLDER_ID**
3. Создайте сервисный аккаунт с ролью `ai.translate.user`
4. Создайте API-ключ → скопируйте **API_KEY**

### Установка ключей:
```bash
export YANDEX_API_KEY="ваш-api-ключ"
export YANDEX_FOLDER_ID="ваш-folder-id"

# Сохранить навсегда:
echo 'export YANDEX_API_KEY="ваш-api-ключ"' >> ~/.zshrc
echo 'export YANDEX_FOLDER_ID="ваш-folder-id"' >> ~/.zshrc
source ~/.zshrc
```

---

## 2️⃣ Ежедневная работа

### Запуск админки:
```bash
cd /Users/johnsky/Documents/balthomes.ru
python3 admin_server.py
```
Откройте: **http://localhost:8000/admin**

### Работа с объектом:
1. **Нажмите "Редактировать"** на объекте
2. **Выберите тип**: Дом / Квартира / Таунхаус
3. **Заполните ВСЕ на русском**:
   - Название
   - Цена
   - Локация
   - Описание
   - Преимущества
   - Характеристики (в зависимости от типа)
4. **Загрузите фото** (можно перетаскивать)
5. **Сохраните**

### Генерация страниц:
```bash
python3 generate_all_pages.py
```

Скрипт автоматически:
- ✅ Переведет на EN, DE, ZH
- ✅ Создаст HTML страницы
- ✅ Сохранит переводы в data.json

### Публикация:
```bash
git add .
git commit -m "Добавлен объект X"
git push origin main
```

Или через админку: **"Отправить на сайт"**

---

## 3️⃣ Типы недвижимости

### 🏡 Дом / Коттедж
Поля:
- Площадь дома (м²)
- Участок (соток)
- Этажей
- Комнат
- Материал стен

### 🏢 Квартира / Апартаменты
Поля:
- Площадь (м²)
- Этаж / Этажность (например: 5/9)
- Комнат
- Подъезд
- Балкон / Лоджия
- Отопление
- Ремонт

---

## 4️⃣ Полезные команды

### Проверка переводов:
```bash
# Посмотреть переводы в data.json
cat data.json | jq '.[] | {id, title}'

# Проверить API ключи
echo $YANDEX_API_KEY
echo $YANDEX_FOLDER_ID
```

### Просмотр сайта локально:
```bash
# Запустить простой веб-сервер
python3 -m http.server 8080

# Откройте http://localhost:8080
```

### Очистка старых страниц:
```bash
# Удалить все object-*.html
rm object-*.html

# Сгенерировать заново
python3 generate_all_pages.py
```

---

## 5️⃣ Структура файлов

```
📁 Проект
├── 📄 admin_server.py          # Админка (запускать)
├── 📄 generate_all_pages.py    # Генератор (запускать)
├── 📄 data.json                # База данных (редактируется через админку)
├── 📁 templates/
│   ├── admin.html              # Интерфейс админки
│   └── full-object-template.html  # Шаблон страницы
├── 📁 images/
│   └── object-{id}/            # Фото каждого объекта
└── 📄 index.html, en.html, de.html, zh.html  # Главные страницы
```

---

## 6️⃣ Частые вопросы

**Q: Как изменить порядок объектов на главной?**
A: В админке перетащите объекты мышкой, затем "Сохранить порядок"

**Q: Переводы неправильные, как исправить?**
A: Отредактируйте вручную в `data.json`, при следующей генерации они сохранятся

**Q: Как добавить новый язык?**
A: Нужно редактировать `generate_all_pages.py` и добавить новый язык в список

**Q: Фото не загружаются?**
A: Проверьте права: `chmod -R 755 images/`

---

## 7️⃣ Лимиты Yandex Cloud

- **1 млн символов/месяц** - БЕСПЛАТНО
- Примерно **500 объектов** в месяц
- После превышения: **1₽ за 1 млн символов**

---

**Готово! Теперь вы можете управлять недвижимостью профессионально! 🎉**
