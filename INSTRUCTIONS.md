# 🏠 Инструкция по управлению контентом Balthomes

Эта памятка поможет вам самостоятельно обновлять объекты недвижимости и менять фотографии на сайте без знания программирования.

---

## 1. Обновление каталога недвижимости (Latest Sales)
Все данные о квартирах и домах находятся в одном файле: `js/properties.js`.

### Как добавить новый объект:
1. Откройте `js/properties.js` в текстовом редакторе.
2. Скопируйте блок данных от `{` до `},` и вставьте его после предыдущего.
3. **Обязательно** укажите:
    - **id**: Следующий порядковый номер.
    - **price**: Цены для всех валют (рубли, евро, юани).
    - **images**: Список из 3-х путей к фото (лежат в папке `images/`).
    - **title, location, stats**: Переводы на все 4 языка (ru, en, de, zh).

**Пример блока:**
```javascript
{
    id: 4,
    price: { ru: "10 000 000 ₽", en: "€ 100,000*", de: "€ 100.000*", zh: "¥ 780,000*" },
    images: ["images/flat4-1.jpg", "images/flat4-2.jpg", "images/flat4-3.jpg"],
    fallbackImages: ["https://images.unsplash.com/..."], 
    title: { ru: "Заголовок", en: "Title", de: "Titel", zh: "标题" },
    location: { ru: "Локация", en: "Location", de: "Lage", zh: "位置" },
    stats: { ru: "Характеристики", en: "Stats", de: "Stats", zh: "特征" }
},
```

---

## 2. Смена фотографий в главном блоке (Hero Slider)
Эти фотографии меняются отдельно в каждом HTML-файле (`index.html`, `en.html`, `de.html`, `zh.html`).

### Как поменять фото:
1. Найдите в HTML коде блок `<div class="hero-img">`.
2. Внутри него находятся теги `<img>`.
3. Чтобы заменить фото, измените путь в `src="..."`.
4. **Важно:** У самой первой картинки должен быть класс `class="hero-slider-img active"`, у остальных — просто `class="hero-slider-img"`.

---

## 3. Настройка уведомлений в Telegram
Если нужно сменить бота или чат, куда приходят заявки, откройте файл: `js/telegram-sender.js`.

Отредактируйте эти строки:
- `const botToken = 'ВАШ_ТОКЕН_БОТА';`
- `const chatId = 'ВАШ_ID_ЧАТА';`

---

## 4. Общие советы
- **Размер фото**: Старайтесь использовать фотографии одного размера (например, 800x600 для каталога и 1000x800 для Hero), чтобы сайт не "прыгал".
- **Папка images**: Всегда загружайте новые файлы фотографий в папку `images/` перед тем, как прописывать их в коде.
- **Синхронизация**: Если вы меняете текст в шапке или подвале, не забывайте вносить эти изменения во все языковые версии сайта.

---
*Сайт работает на "чистом" коде, поэтому изменения вступают в силу сразу после сохранения файлов и обновления страницы в браузере.*
